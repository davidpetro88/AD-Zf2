
<table id="list" class="table table-hover table-striped">
<thead>
    <tr>
        <th class="col-xs-11">Id</th>
        <th class="col-xs-1"><a href="<?php echo $this->acploUrl()->from('proposta-servico/add',[],true)?>" class="btn btn-sm btn-success pull-right"><?= $this->acploIcon('glyphicon-plus') ?></a></th>
    </tr>
</thead>
<tbody>
    <?php foreach ($this->paginator as $entity):?>
        <tr class="linha">
            <td><?= $entity->getId()?></td>
            <td class="text-right">
                <a href="<?php echo $this->acploUrl()->from('proposta-servico/edit',['id'=>$entity->getId()], true)?>" class="btn btn-xs btn-warning"><?= $this->acploIcon('glyphicon-edit') ?></a>
                <a href="<?php echo $this->acploUrl()->from('proposta-servico/delete',['id'=>$entity->getId()], true)?>" class="btn btn-xs btn-danger"><?= $this->acploIcon('glyphicon-trash') ?></a>
            </td>
        </tr>
    <?php endforeach;?>
</tbody>
</table>

<?php if ($this->paginator->count() > 1):?>
<div class="row">
    <div class="col-xs-12 text-center"><?= $this->losPaginationControl($this->paginator,'Sliding',null,['query'=>$this->query]); ?></div>
</div>
<?php endif?>
