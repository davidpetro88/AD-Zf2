<div class="row">
	<div class=" col-lg-2"></div>
	<div class=" col-lg-7">
		<div class="panel">
			<div class="panel-heading">
				<div class="panel-title">
					<h3>
						<i class="glyphicon glyphicon-list"></i> <?php echo $this->translate('Forma de Pagamento'); ?></h3>
				</div>
			</div>
			<div class="panel-body">
				<table id="list" class="table table-hover table-striped">
					<thead>
						<tr> <!-- 1,2,1,1,2 -->
							<th class="col-xs-4">Nome</th>
							<th class="col-xs-1">Ativo</th>
							<th class="col-xs-2"><a
								href="<?php echo $this->losUrl()->from('forma-pagamento/add', [], true) ?>"
								class="btn btn-sm btn-success pull-right"><?= $this->losIcon('glyphicon-plus') ?></a></th>
						</tr>
					</thead>
					<tbody>
			<?php foreach ($this->paginator as $entity): ?>
    			<tr class="linha">
							<td> <?= $entity->getNome() ?></td>
							<td> <?= ($entity->getAtivado() == 1) ? 'Sim' : 'Não';  ?> </td>
							<td class="text-right"><a
								href="<?php echo $this->losUrl()->from('forma-pagamento/edit', ['id' => $entity->getId()], true) ?>"
								class="btn btn-xs btn-warning"><?= $this->losIcon('glyphicon-edit') ?></a>
								<a
								href="<?php echo $this->losUrl()->from('forma-pagamento/delete', ['id' => $entity->getId()], true) ?>"
								class="btn btn-xs btn-danger"><?= $this->losIcon('glyphicon-trash') ?></a>
							</td>
						</tr>
			<?php endforeach; ?>
		    </tbody>
				</table>

				<div class="panel-footer">
		    <?php if ($this->paginator->count() > 1): ?>
    		    <div class="row">
						<div class="col-xs-12 text-center"><?= $this->losPaginationControl($this->paginator, 'Sliding', null, ['query' => $this->query]); ?></div>
					</div>
		    <?php endif ?>
		</div>
			</div>
		</div>
	</div>
	<div class=" col-lg-1"></div>
</div>